import { StreetMaskDirective } from './street-mask.directive';

describe('StreetMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new StreetMaskDirective();
    expect(directive).toBeTruthy();
  });
});
