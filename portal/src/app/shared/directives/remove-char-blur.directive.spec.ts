import { RemoveCharBlurDirective } from './remove-char-blur.directive';

describe('RemoveCharBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveCharBlurDirective();
    expect(directive).toBeTruthy();
  });
});
