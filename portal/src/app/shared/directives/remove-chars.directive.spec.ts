import { RemoveCharsDirective } from './remove-chars.directive';

describe('RemoveCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveCharsDirective();
    expect(directive).toBeTruthy();
  });
});
