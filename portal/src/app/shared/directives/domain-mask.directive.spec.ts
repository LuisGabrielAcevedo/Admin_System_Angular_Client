import { DomainMaskDirective } from './domain-mask.directive';

describe('DomainMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DomainMaskDirective();
    expect(directive).toBeTruthy();
  });
});
