import { ChassisMaskDirective } from './chassis-mask.directive';

describe('ChassisMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new ChassisMaskDirective();
    expect(directive).toBeTruthy();
  });
});
