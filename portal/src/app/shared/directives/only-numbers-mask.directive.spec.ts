import { OnlyNumbersMaskDirective } from './only-numbers-mask.directive';

describe('OnlyNumbersMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumbersMaskDirective();
    expect(directive).toBeTruthy();
  });
});
